query Bugs($searchProduct: BugSearch!, $paging: Paging) {
  bug: bugs(search: $searchProduct, paging: $paging) {
    pageInfo {
      hasNextPage
      nextPage
    }
    edges {
      node {
        assignedTo {
          name
        }
        tags
        component
        lastChanged
        id
        summary
        status
        resolution
        url
      }
    }
  }
}